import { Router } from 'express';

const router = Router();

// Inventory routes placeholder
// router.patch('/stock', inventoryController.updateStock);
// router.get('/ledger/:productId', inventoryController.getLedger);

export default router;
